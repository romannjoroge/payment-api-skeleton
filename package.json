{
  "name": "payment-api-skeleton",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "npx nodemon",
    "start": "node dist/index.mjs",
    "build": "rm -rf dist && tsup",
    "push": "pnpm drizzle-kit push",
    "generate": "pnpm drizzle-kit generate",
    "migrate": "pnpm drizzle-kit migrate",
    "studio": "pnpm drizzle-kit studio",
    "coverage": "jest --coverage",
    "test": "jest",
    "seed:industries": "tsx src/lib/seedIndustries.ts",
    "email": "email dev --dir src/lib/emails/templates -p 3001",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.7.0",
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/jest": "^30.0.0",
    "jest": "^30.2.0",
    "nodemon": "^3.1.11",
    "pino-pretty": "^13.1.3",
    "prettier": "^3.7.4",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "pino": "^10.1.0"
  }
}
